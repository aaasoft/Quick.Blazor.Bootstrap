@using Quick.Fields

<div class="@Field.Html_Class" style="@Field.Html_Style">
    @Field.Value
    @if (Field.Children != null)
    {
        @foreach (var child in Field.Children)
        {
            <Control Field="@child" />
        }
    }
</div>

@code
{
    [Parameter]
    public FieldForGet Field { get; set; }
}